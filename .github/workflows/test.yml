name: hello-world
on: push
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout - Test files repo
        uses: actions/checkout@v4.1.2
      - name: List
        run: |
          ls -lah
          echo "perro" > log.txt
